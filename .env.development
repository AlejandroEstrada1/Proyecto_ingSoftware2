# Desarrollo: sin VITE_API_URL el front usa rutas relativas (/auth, /products…)
# y Vite hace proxy a Express (evita puerto API equivocado y CORS).
# Si Express no está en 3001, crea `.env.development.local` con:
# VITE_PROXY_TARGET=http://127.0.0.1:PUERTO
